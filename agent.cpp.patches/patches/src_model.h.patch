diff --git a/src/model.h b/src/model.h
--- a/agent.cpp/src/model.h
+++ b/agent.cpp/src/model.h
@@ -36,6 +36,10 @@ struct ModelConfig
     // Optional GBNF grammar and root rule name
     std::string grammar;
     std::string grammar_root = "root";
+    // llamafile: ask the chat template to enable model reasoning
+    // (<think> blocks). Off by default: agent turns favor latency, and
+    // reasoning is extracted to reasoning_content when it appears anyway.
+    bool enable_thinking = false;
 };
 
 /// Reads a GBNF file into a string for ModelConfig::grammar
@@ -206,10 +210,10 @@ class Model
 
     std::shared_ptr<ModelWeights> weights_;
     llama_context* ctx_ = nullptr;
-    llama_sampler* sampler_ = nullptr;
-    // Non-owning pointer to the grammar sampler in sampler_'s chain
-    // Reset this one between turns without resetting the rest of the chain
-    llama_sampler* grammar_sampler_ = nullptr;
+    // Grammar-aware sampler (llamafile: common_sampler instead of a raw
+    // llama_sampler chain, so chat-template tool-call grammars and the
+    // user's GBNF grammar both constrain sampling).
+    struct common_sampler* sampler_ = nullptr;
     std::vector<llama_token> processed_tokens_; // Track tokens in KV cache
     int n_past_ = 0;                            // Track position in KV cache
     ModelConfig config_;
